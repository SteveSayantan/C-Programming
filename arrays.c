//Syntax for Declaring and Initializing an array

/* 

    Data_type name[size]; //Declaration
    name[0]= value; //Initialization
    name[1]=value;

    Data_type name [] ={x,y,z...} //Declaration and Initialization in the same line. In this case, specifying the size is not mandatory.

    Data_type name [rows][cols] ={{a,b,c},{x,y,z}} ; //Declaration and initialization for 2D arrays, specifying 'cols' is mandatory, even while passing a 2D array as a formal parameter

 */

